<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/products/<%= selectedProduct._id %>?_method=PUT" method="POST">
        <label for="name-input">Product Name:</label>
        <input id="name-input" type="text" placeholder="product" name="name" value="<%= selectedProduct.name %>">
        <br>
        <label for="name-input">Product Price:</label>
        <input id="price" type="number" name="price" placeholder="price" value="<%= selectedProduct.price %>">
        <br>
        <label for="product-category">Product Category: </label>
        <select name="category" id="product-category">
            <% for (let category of categories) {%>
                <!-- Adds 'selected' to the option if loop category matches the selected category for edit -->
                <option value="<%= category %>"<%= category === selectedProduct.category ? 'selected' : '' %>><%= category %></option>
            <% } %>
        </select>        
        <br>
        <button>Submit</button>
    </form>
    <a href="/products/<%= selectedProduct._id %>">BACK TO DETAILS</a>
</body>
</html>